# Normalise line endings to LF in the repository for cross-platform consistency.
* text=auto eol=lf

# Treat these as binary (no line-ending conversion).
*.webp binary
*.jpg binary
*.jpeg binary
*.png binary
*.svg binary
